# Dependências exclusivas do pipeline de CI — não usar em runtime.
# Versões fixadas e validadas com Python 3.11 para este baseline.
# Atualizar somente após confirmar compatibilidade com o interpretador-alvo.
#
# Justificativa de cada versão:
#   pip-audit 2.7.3       — release estável; suporte a PURL e SBOM integrado
#   bandit[toml] 1.7.10   — release estável; sem breaking changes na série 1.7.x
#   pytest 8.3.4          — release estável; API de fixtures/markers estável
#   pytest-asyncio 0.24.0 — compatível com pytest >=8.1 e asyncio nativo
#   httpx 0.27.2          — cliente HTTP para testes de integração; API estável
#
# Instalação seletiva por job (princípio de menor dependência):
#   pip-audit job  → pip install "pip-audit==2.10.1"
#   bandit job     → pip install "bandit[toml]==1.7.10"
#   test job       → pip install "pytest==8.3.4" "pytest-asyncio==0.24.0" "httpx==0.27.2"
#
# Este arquivo serve como registro centralizado das versões canônicas.
# O workflow instala apenas as dependências necessárias por job.
pip-audit==2.10.1
bandit[toml]==1.7.10
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2
